<!-- Title: match the branch — feat/, fix/ or chore/ prefix. -->

## What this changes (for the Technical Product Owner)

- <!-- behaviour a visitor would notice — no implementation detail -->

## What gets measured

<!-- The GA4 events this adds or touches (track() in src/lib/analytics.js), or one
     line saying why none: a build script nobody sees, a data fix, already counted.
     Names are snake_case. Params carry entity refs and F1 search terms — never PII,
     never free-text feedback or emails. -->

## Detail

<!-- implementation notes: approach, docs amended (if any),
     anything deliberately NOT done -->

## Gates

<!-- npm test result, and npm run build if this touches Astro/islands/routes/build
     scripts. Judged on exit codes, not piped output. Note dark + light theme checked
     for any UI change. -->
