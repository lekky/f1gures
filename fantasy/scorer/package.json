{
  "name": "f1gures-fantasy-scorer",
  "version": "0.2.0",
  "private": true,
  "description": "Scheduled job that scores the f1gures fantasy game from the public season bundle into PocketBase.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node run.mjs",
    "dry-run": "node run.mjs --dry-run",
    "test": "cd ../.. && npx vitest --run fantasy/scorer"
  }
}
